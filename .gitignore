# Swift Package Manager
.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
# Package.resolved is committed so release builds pin the exact Sparkle version.

# Release build output (the .app bundle and DMGs from scripts/build_app.sh).
dist/

# Xcode / DerivedData
DerivedData/
build/
*.xcuserstate
xcuserdata/

# Swift / compiler artifacts
*.o
*.swiftmodule
timeline.xctimeline

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Editor / IDE (keep the shared VS Code launch config)
.vscode/*
!.vscode/launch.json
.idea/
*.swp
*~

# Generated verification artifacts — screenshots/appicon are kept for the README;
# ignore the transient logs and the non-deterministic live screen capture.
verify-artifacts/*.log
verify-artifacts/screenshot-live.png
